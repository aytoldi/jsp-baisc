<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">
    <servlet>
        <!-- 声明Servlet对象 -->
        <servlet-name>TestServlet</servlet-name>
        <!-- 上面一句指定Servlet对象的名称 -->
        <servlet-class>xml.TestServlet</servlet-class>
        <!-- 上面一句指定Servlet对象的完整位置，包含包名和类名 -->
    </servlet>
    <servlet-mapping>
        <!-- 映射Servlet -->
        <servlet-name>TestServlet</servlet-name>
        <!--<servlet-name>与上面<Servlet>标签的<servlet-name>元素相对应，不可以随便起名  -->
        <url-pattern>/WebXmlServlet</url-pattern>
        <!-- 上面一句话用于映射访问URL -->
    </servlet-mapping>
    <servlet>
<!--        http://localhost:8080/basic_war_exploded/session/cart.jsp-->
        <!-- 声明Servlet对象 -->
        <servlet-name>CartServlet</servlet-name>
        <!-- 上面一句指定Servlet对象的名称 -->
        <servlet-class>session.CartServlet</servlet-class>
        <!-- 上面一句指定Servlet对象的完整位置，包含包名和类名 -->
    </servlet>
    <servlet-mapping>
        <!-- 映射Servlet -->
        <servlet-name>CartServlet</servlet-name>
        <!--<servlet-name>与上面<Servlet>标签的<servlet-name>元素相对应，不可以随便起名  -->
        <url-pattern>/CartServlet</url-pattern>
        <!-- 上面一句话用于映射访问URL -->
    </servlet-mapping>
    <servlet>
        <!-- 声明Servlet对象 -->
        <servlet-name>LoginServlet</servlet-name>
        <!-- 上面一句指定Servlet对象的名称 -->
        <servlet-class>session.LoginServlet</servlet-class>
        <!-- 上面一句指定Servlet对象的完整位置，包含包名和类名 -->
    </servlet>
    <servlet-mapping>
        <!-- 映射Servlet -->
        <servlet-name>LoginServlet</servlet-name>
        <!--<servlet-name>与上面<Servlet>标签的<servlet-name>元素相对应，不可以随便起名  -->
        <url-pattern>/LoginServlet</url-pattern>
        <!-- 上面一句话用于映射访问URL -->
    </servlet-mapping>
    <!--el表达式-->
    <servlet>
        <!-- 声明Servlet对象 -->
        <servlet-name>StudentServlet</servlet-name>
        <!-- 上面一句指定Servlet对象的名称 -->
        <servlet-class>el.StudentServlet</servlet-class>
        <!-- 上面一句指定Servlet对象的完整位置，包含包名和类名 -->
    </servlet>
    <servlet-mapping>
        <!-- 映射Servlet -->
        <servlet-name>StudentServlet</servlet-name>
        <!--<servlet-name>与上面<Servlet>标签的<servlet-name>元素相对应，不可以随便起名  -->
        <url-pattern>/StudentServlet</url-pattern>
        <!-- 上面一句话用于映射访问URL -->
    </servlet-mapping>
</web-app>
